version: "2"
plugins:
  - name: ts
    wasm:
      url: https://downloads.sqlc.dev/plugin/sqlc-gen-typescript_0.1.3.wasm
      sha256: 287df8f6cc06377d67ad5ba02c9e0f00c585509881434d15ea8bd9fc751a9368
sql:
  - schema: "db/migrations"
    queries: "app/features/auth/queries/queries.sql"
    engine: postgresql
    codegen:
      - out: app/features/auth/queries
        plugin: ts
        options:
          runtime: node
          driver: postgres
  - schema: "db/migrations"
    queries: "app/features/companies/queries/queries.sql"
    engine: postgresql
    codegen:
      - out: app/features/companies/queries
        plugin: ts
        options:
          runtime: node
          driver: postgres
  - schema: "db/migrations"
    queries: "app/features/companies/queries/membership-queries.sql"
    engine: postgresql
    codegen:
      - out: app/features/companies/queries
        plugin: ts
        options:
          runtime: node
          driver: postgres
  - schema: "db/migrations"
    queries: "app/features/jobs/queries/queries.sql"
    engine: postgresql
    codegen:
      - out: app/features/jobs/queries
        plugin: ts
        options:
          runtime: node
          driver: postgres
  - schema: "db/migrations"
    queries: "app/features/applications/queries/queries.sql"
    engine: postgresql
    codegen:
      - out: app/features/applications/queries
        plugin: ts
        options:
          runtime: node
          driver: postgres
  - schema: "db/migrations"
    queries: "app/features/candidates/queries/queries.sql"
    engine: postgresql
    codegen:
      - out: app/features/candidates/queries
        plugin: ts
        options:
          runtime: node
          driver: postgres
  - schema: "db/migrations"
    queries: "app/features/notifications/queries/queries.sql"
    engine: postgresql
    codegen:
      - out: app/features/notifications/queries
        plugin: ts
        options:
          runtime: node
          driver: postgres
  - schema: "db/migrations"
    queries: "app/features/pre-evaluations/queries/queries.sql"
    engine: postgresql
    codegen:
      - out: app/features/pre-evaluations/queries
        plugin: ts
        options:
          runtime: node
          driver: postgres
  - schema: "db/migrations"
    queries: "app/features/reports/queries/queries.sql"
    engine: postgresql
    codegen:
      - out: app/features/reports/queries
        plugin: ts
        options:
          runtime: node
          driver: postgres
  - schema: "db/migrations"
    queries: "app/features/interviews/queries/queries.sql"
    engine: postgresql
    codegen:
      - out: app/features/interviews/queries
        plugin: ts
        options:
          runtime: node
          driver: postgres
  - schema: "db/migrations"
    queries: "app/features/batches/queries/queries.sql"
    engine: postgresql
    codegen:
      - out: app/features/batches/queries
        plugin: ts
        options:
          runtime: node
          driver: postgres
  - schema: "db/migrations"
    queries: "app/features/feedback/queries/queries.sql"
    engine: postgresql
    codegen:
      - out: app/features/feedback/queries
        plugin: ts
        options:
          runtime: node
          driver: postgres
  - schema: "db/migrations"
    queries: "app/features/dashboard/queries/queries.sql"
    engine: postgresql
    codegen:
      - out: app/features/dashboard/queries
        plugin: ts
        options:
          runtime: node
          driver: postgres
